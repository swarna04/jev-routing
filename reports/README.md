# Reports

`pnpm eval` writes `latest.md` and `latest.json` here.

- Mock scripts demonstrate the scorer. They are not live-model results.
- Live `bare_llm`, `constrained_llm`, and `jev` numbers are included only when those runs actually happen.
- Do not paste fabricated percentages into this folder.
